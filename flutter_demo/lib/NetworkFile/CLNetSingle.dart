import 'package:flutter/material.dart';

class CLNetSingle extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(
          '网络',
        ),
      ),
      body: Center(
        child: Text('网络相关'),
      ),
    );
  }
}